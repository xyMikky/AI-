---
description: 向量检索集成规范 — 主控中心及任意模块在需要"找最相关记录"时必须先调向量检索,不得盲读整个参考档案
alwaysApply: true
---

# 向量检索集成规范(Vector Search Integration)

## 核心原则

**参考库 / P 域 / 品牌规范已全量向量化**(787 条切片,Qwen3-Embedding-8B,索引位于 `工具/向量检索/vector_index/`)。任何时候需要"从档案中找最相关的 N 条记录 / 案例 / 原则 / 品牌条款"时,**必须优先调用向量检索**,而不是线性阅读整个子文件后自行判断。

**定位**: 向量检索是主控中心 ④ 阶段门(【参考精炼摘要 B】提炼)的**默认召回通道**,也是 P 域案例复用(③.5)和品牌规范按需抽取的**底层引擎**。

---

## 何时必须调用(强制触发)

| 场景 | 触发门 | 调用目的 |
|---|---|---|
| 主控中心生成【参考精炼摘要 B】 | 主控中心 ④ 阶段门 | 召回 top-K 候选记录,再从中选 2-3 条写入摘要 |
| 主控中心 P 域复用检查(③.5) | P 域门 | 按任务描述找最相似的 P-XXX 成功案例 |
| 品牌被激活后,需找特定规范条款 | 品牌加载后 | 从 BRAND 类切片中定位具体规则(色彩/字体/Logo/语气/禁忌) |
| 用户问"有没有类似案例/类似设计/类似原则" | 任意对话 | 直接召回,避免"我记得好像有..."式的主观回忆 |
| M10/M17 学习任务中判断新素材与现有记录是否重复 | 学习前去重 | 召回相似度 ≥ 0.7 的记录作为参考 |
| M8/M15 构造 Prompt 前寻找"可迁移原则" | Prompt 构造前 | 召回 PRINCIPLE 类切片,提取直接可用的设计原则 |

---

## 何时可以跳过(豁免)

| 场景 | 原因 |
|---|---|
| 用户明确指定了参考档案/案例编号(如"用 P-003 的 Prompt") | 定位已精准,无需检索 |
| 纯文档类任务(提案/规范撰写)且不涉及设计决策 | 无召回需求 |
| 用户直接提供了完整的素材和需求,无需从档案中找灵感 | 无召回需求 |
| 系统维护任务(M10/M17 的学习主流程/M18 诊断/架构师) | 任务本身不依赖参考召回 |
| 向量索引文件缺失(`工具/向量检索/vector_index/vectors.npz` 不存在) | 降级为原有线性阅读流程,并提示用户"建议运行 `python 工具/向量检索/build_index.py`" |

---

## 标准调用语法

### 常规召回(AI 消费)

```powershell
python 工具/向量检索/search.py "查询描述" --top 6 --json
```

### 按域过滤(找特定域的记录)

```powershell
# 只在 E 域搜
python 工具/向量检索/search.py "促销Banner版式" --top 5 --domain E_平面与海报 --json

# 只找品牌规范条款
python 工具/向量检索/search.py "NEBILITY 主色值" --top 3 --domain 品牌规范 --json

# 只找通识原则
python 工具/向量检索/search.py "构图留白比例" --top 5 --domain 00_通识原则 --json
```

### 按切片类型过滤

```powershell
# 只找 P 域成功案例
python 工具/向量检索/search.py "女士塑身衣模特场景" --type P_CASE --top 3 --json

# 只找通识原则(避免大段档案干扰)
python 工具/向量检索/search.py "色彩节制高级感" --type PRINCIPLE --top 5 --json

# 只找品牌规范章节
python 工具/向量检索/search.py "字体使用规范" --type BRAND --top 5 --json
```

### 拉取完整正文(深度使用时)

```powershell
python 工具/向量检索/search.py "查询" --top 3 --json --full
```

> `--full` 返回的 `text` 字段是**已切片 + 档案头上下文**的完整正文,可直接作为【参考精炼摘要 B】的"可迁移原则"来源,**不需要再去读原档案文件**。

### Query 改写(口语化需求 → 结构化检索串)⚠️ 推荐默认开启

当 query 是用户的原始口语化描述(如"帮我做个好看的促销图 NEBILITY 塑身衣 美国市场"),**应加 `--rewrite` 让检索器先做术语扩展**,自动追加英文对照词、领域同义词、平台/地域/品类关键词:

```powershell
# 自动识别任务类型/品牌/品类/地域/平台并扩展术语
python 工具/向量检索/search.py "帮我做个好看的促销图 NEBILITY 塑身衣 美国市场" --rewrite --top 5 --json
```

**效果对比**(实测同一个 query):

| 场景 | 不加 --rewrite | 加 --rewrite |
|---|---|---|
| Top5 主题准确率 | 3/5(混入咖啡/首饰等误中) | **5/5 全命中** |
| 运动内衣/促销直接案例召回 | 第 5 位或未出现 | **进 Top2** |

**使用准则**:
- 用户原话、自然语言描述 → **加 `--rewrite`**
- 已经是结构化术语串(如"欧美电商促销结构版式") → 可不加(加了也不降质)
- 低召回率(top-1 < 0.55)时 → **追加 `--rewrite` 重查一次**

**独立使用改写器**(不做检索,只想看扩展后的 query):

```powershell
python 工具/向量检索/query_rewriter.py "你的口语化查询" --json
```

改写器覆盖的维度:
- **task_type** — 促销图/Banner/产品主图/详情页/Logo/配色/字体/换背景 等
- **brand** — 自动识别 `品牌规范/` 下已注册的品牌名
- **product** — 塑身衣/运动内衣/塑形裤/泳装 等品类
- **region** — 美国/欧美/日本/中国/东南亚 等
- **platform** — Amazon/Shopify/DTC/天猫/小红书 等
- **style** — 高级感/简约/复古/温馨 等调性同义词

### Rerank 精排(cross-encoder)⚠️ 强相关任务建议开启

向量检索是"双塔近似":query 与 doc 各自编码后算相似度,快但粗。Rerank 是"交叉编码器":两者成对输入模型捕获 token 级交互,**精度显著高于向量粗排**,代价是单次调用 +¥0.001~0.003。

```powershell
# 完整推荐组合:改写 + 精排 + 完整正文
python 工具/向量检索/search.py "你的查询" --rewrite --rerank --top 5 --json --full

# 加大粗排候选池(默认 20,复杂任务可提到 40,精度更高但成本更大)
python 工具/向量检索/search.py "你的查询" --rerank --rerank-pool 40 --top 5 --json
```

**实测对比**(同一个 query "帮我做个好看的促销图 NEBILITY 塑身衣 美国市场"):

| 排名 | 仅改写 | 改写 + rerank |
|---|---|---|
| 1 | P2 分类索引(0.785) | **NEBILITY 广告投放#5**(rerank=0.975) |
| 2 | 广告投放#1(0.764) | **P-003 NEBILITY 塑身短裤 HERO**(0.971) |
| 3 | 广告投放#0(0.762) | **P-001 NEBILITY 塑身背心促销**(0.968) |
| 4 | 广告投放#2(0.750) | **P-004 NEBILITY 塑身短裤 DTC**(0.965) |

**核心收益**:P 域 NEBILITY 塑身衣促销成功案例从 Top5 仅 1 条 → **Top5 命中 4 条**(最值钱的可复用 Prompt 直接出现在主位)。

**使用准则**:

| 场景 | 是否开启 rerank |
|---|---|
| 主控中心 ④ 生成【参考精炼摘要 B】/ ③.5 P 域复用检查 | **✅ 强烈推荐开启** |
| 品牌关键条款精查(色值/字体/Logo) | ✅ 开启,粗排池可减到 10 |
| 纯探索式检索("有没有类似案例") | ✅ 开启 |
| 学习任务去重比对(M10/M17 内部) | ❌ 不开启(成本敏感,粗排已够) |
| 已确认采纳具体 chunk_id 后的正文拉取 | ❌ 不开启 |
| API 故障 / 降级模式 | ❌ 自动降级(失败时 search 会回退为向量粗排,stderr 输出告警) |

**输出字段**(开启 rerank 时 JSON 会多两个字段):

```json
{
  "rank": 1,
  "score": 0.9749,           // rerank 阶段的最终分(0-1)
  "rerank_score": 0.9749,    // 同 score
  "vector_score": 0.7207,    // 向量粗排阶段的分数(仅供观测)
  "chunk_id": "...",
  ...
}
```

**Rerank 分数参考线**(与向量分不同!):

| rerank 分数 | 含义 |
|---|---|
| ≥ 0.95 | 极高相关(语义匹配近乎完美) |
| 0.80 – 0.95 | 强相关(直接可用) |
| 0.50 – 0.80 | 相关(可作候选) |
| 0.20 – 0.50 | 弱相关(概念参考) |
| < 0.20 | 几乎不相关 |

---

## 输出格式(AI 解析指南)

`--json` 模式返回:

```json
{
  "query": "原始查询",
  "n_results": 6,
  "results": [
    {
      "rank": 1,
      "score": 0.66,
      "chunk_id": "E2a1_模特主导促销版式::0",
      "domain": "E_平面与海报",
      "type": "RECORD",
      "source_path": "参考库\\E_平面与海报\\E2a1_模特主导促销版式.txt",
      "chars": 5751,
      "est_tokens": 5769,
      "preview": "..."
    }
  ]
}
```

### 分数参考线(score 为余弦相似度)

| 分数区间 | 含义 | 建议处置 |
|---|---|---|
| ≥ 0.70 | 强相关 | 直接采纳为主要参考 |
| 0.55 – 0.70 | 相关 | 作为候选池,再筛选 |
| 0.40 – 0.55 | 弱相关 | 仅参考概念,不作结构参考 |
| < 0.40 | 几乎不相关 | 忽略 |

**跨域召回是优势而非噪声**:检索 "促销Banner" 同时召回 E 域版式记录 + P3 信息层级通识原则 + P 域成功案例,这正是"多轴协同"的设计理念。

---

## 在交接卡 B 中的使用流程

主控中心生成【参考精炼摘要 B】时的新流程:

```
① 从调控中心决策已确定主加载档案(如 E_平面与海报 + B_配色档案)
  ↓
② 调用向量检索,得到 top-6 候选切片(JSON)
  ⚠️ 若 query 源自用户口语化需求(含"帮我"、"做个"、"好看的"等):加 --rewrite
  ⚠️ 生成交接卡 B 属于强相关任务:加 --rerank(精度关键,成本仅约 ¥0.002)
  python 工具/向量检索/search.py "[任务核心描述]" --top 6 --json --full --rewrite --rerank
  ↓
③ 从候选切片中筛选 2-3 条:
   - score ≥ 0.55 优先
   - 覆盖不同 domain(E域案例 + 通识原则 + 品牌条款 + P 域成功案例)
   - 读取切片的 text 字段,提取「可迁移原则」段落
  ↓
④ 从选中切片的 source_path 提取图片路径(拼接为 参考库/[域]/原始素材/[文件名])
  ↓
⑤ 组装【参考精炼摘要 B】,标注各切片的借鉴维度
```

---

## ❌ 禁止行为

- **在已向量化的三大知识体系(参考库 / P 域 / 品牌规范)中,线性阅读整个子文件后自行筛选 2-3 条**(浪费上下文且精度低于向量检索)
- **以"文件不大我直接读了也快"为由跳过向量检索**(浪费的是模型上下文预算,不是时间)
- **调用后不按 score 过滤,把低分结果也写入【参考精炼摘要 B】**(< 0.40 的结果应丢弃)
- **只看 preview 字段就下结论**,在深度使用时不拉取 `--full` 查看完整正文
- **对文件系统已有的图片路径自行拼接,不走 source_path 字段**(source_path 是经过文件存在性验证的权威路径)

## ✅ 正确行为

- 主控中心 ④ 阶段门触发时,立即执行一次 `search.py ... --json --full` 召回
- 优先使用默认 `--exclude-type INDEX`(索引文件摘要对设计任务价值低)
- 品牌任务中,对品牌档案条款的查找一律走向量检索(`--domain 品牌规范`)
- 在【参考精炼摘要 B】中标注"来源: 向量检索 score=0.66",让决策链路可追溯
- 若 top 结果分数整体偏低(top-1 < 0.40),先向用户说明"档案中未找到强相关案例,改用通识原则兜底"再决定下一步
- 调用失败(索引缺失)时降级为原线性阅读流程,并在回复中注明"建议运行 `python 工具/向量检索/build_index.py` 构建索引"

---

## 快速命令速查

```powershell
# 通用:找最相关 5 条
python 工具/向量检索/search.py "你的查询" --json

# 用户口语化查询 → 先改写再检索(推荐)
python 工具/向量检索/search.py "你的口语化查询" --rewrite --top 5 --json

# 【最强配置】改写 + 精排 + 完整正文 — 用于主控中心 ④/③.5 阶段门
python 工具/向量检索/search.py "你的查询" --rewrite --rerank --top 5 --json --full

# 仅通识原则(轻量召回)
python 工具/向量检索/search.py "你的查询" --type PRINCIPLE --top 5 --json

# 仅品牌规范条款
python 工具/向量检索/search.py "你的查询" --domain 品牌规范 --top 3 --json --full

# P 域成功案例复用
python 工具/向量检索/search.py "你的查询" --type P_CASE --top 3 --json --full

# 召回 + 完整正文(准备直接喂给 Prompt 构造)
python 工具/向量检索/search.py "你的查询" --top 3 --json --full
```

## 与其他规则的关系

| 规则 | 关系 |
|---|---|
| `design-must-use-main-control` | 本规范是主控中心 ④ 阶段门的默认召回通道 |
| `ref-image-must-include` | 向量检索返回的 source_path 可直接作为图片路径来源 |
| `p-domain-save` | P 域入库后,新案例下次即被检索召回(前提:重建/增量更新索引) |
| `brand-spec-loading` | 品牌激活后,BRAND 类切片的按需抽取走向量检索 |
