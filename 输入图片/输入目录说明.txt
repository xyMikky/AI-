# 输入图片 · 用户素材入口

把本轮任务要用的原始素材（产品图 / 参考图 / 版式图 / Logo 等）放在本目录，再在对话中引用。

## 约定

- 建议命名：`[品牌或项目]-[用途]-[简述].png`，便于在 Prompt 中对应 `image N`
- 生图调用时通过 `--images` 传入本目录或参考库中的图片路径
- 本目录图片已在 `.gitignore` 中忽略（属临时输入，不入版本库）；仅保留本说明与 `.gitkeep`
- 生成的产物不要放这里，统一落在 `生成结果输出/`（见该目录的「输出目录说明.txt」）
