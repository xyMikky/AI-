---
description: 
alwaysApply: true
---

# P域入库规则（双轨归档强制执行）

## ⚡ 最高优先级触发词识别

以下任意触发词出现，**立即强制执行本规则，优先级高于其他一切保存/归档行为**：

> "保存这次生成" / "存入案例库" / "入库" / "保存成功案例" / "把这个案例存起来" / "记录这次的Prompt" / "复用库入库" / "把这个Prompt存起来" / "P域入库" / "存P域"

⚠️ **"存入案例库" 明确指P域生图成功案例库（Prompt Reuse Library），不是品牌设计案例文件夹。"案例库" = P域 = `参考库/P_生图成功案例库/`**

---

## 三轨强制执行顺序

**轨道 A 必须先执行，轨道 B 在 A 完成后执行，轨道 C 在 A/B 之后兜底执行。禁止只做 B 不做 A，也禁止跳过 C。**

```
触发词被识别
 ↓
【轨道 A：P域入库】← 永远执行，无论品牌是否激活
 ↓
【轨道 B：品牌设计案例归档】← 仅当品牌激活时执行（在 A 完成后执行）
 ↓
【轨道 C：向量索引增量更新】← 永远执行（运行 python 工具/向量检索/update_index.py）
 ↓
输出三轨完成确认摘要
```

---

## 轨道 A：P域入库（永远执行）

### ⚠️ Prompt获取优先级（执行前必读）

P域入库的核心价值是保存完整Prompt以供复用。如果Prompt不在当前上下文：
- **不得静默跳过P域入库**
- **必须明确告知用户**：「检测到P域入库触发。当前对话上下文中未找到本次生图的完整Prompt。请提供：(1) 本次生图使用的完整英文Prompt；(2) 使用了哪些参考图（image 1/2/3...各是什么）」
- 等用户提供后，再继续执行步骤

---

### 步骤 A1：确认所有必要信息

从当前对话中提取（有则取，无则向用户确认）：
- 任务类型：P1 模特场景图 / P2 促销Banner / P3 产品展示图 / P4 详情页模块 / P5 白底产品图
- 品牌名（如有）、产品品类、投放平台
- **完整英文Prompt**（不得省略，这是P域最核心的资产）
- 本次 `--images` 参考图清单（各图文件名 + 角色说明）
- 生成结果图路径（`生成结果输出/` 下的完整文件名）
- 宽高比、分辨率

---

### 步骤 A2：读取P域索引，分配编号

```powershell
$root = $PWD.Path
$pIndex = Join-Path $root "参考库\P_生图成功案例库\P_域索引.txt"
# Read P_域索引.txt → 从「案例文件夹完整列表」取最大编号 +1
# 格式：P-001, P-002, P-003...（三位数补零）
```

---

### 步骤 A3：创建案例文件夹

**命名规则**：`P-[编号]-[品牌]-[产品简称]-[任务类型缩写]-[YYYYMMDD]`

```powershell
$root = $PWD.Path
$caseFolder = "P-001-NEBILITY-男士塑身背心-促销Banner-20260416"  # 示例
$casePath = Join-Path $root "参考库\P_生图成功案例库\$caseFolder"
New-Item -ItemType Directory -Force -Path $casePath | Out-Null
```

---

### 步骤 A4：复制所有参考图到案例文件夹

**命名规则**：`img[N]-[简短描述].[ext]`（N 对应 image 1/2/3...的顺序）

```powershell
# ⚠️ 路径含中文，必须用 [System.IO.File]::Copy() 而非 Copy-Item
# 示例：
[System.IO.File]::Copy("$root\输入图片\产品图.jpg", "$casePath\img1-产品图.jpg", $true)
[System.IO.File]::Copy("$root\参考库\人物库\欧美_白皙\XXX.png", "$casePath\img2-人物库-欧美白皙女.png", $true)
[System.IO.File]::Copy("$root\参考库\场景库\室内\XXX.jpg", "$casePath\img3-场景库-室内客厅.jpg", $true)

# 结果图
[System.IO.File]::Copy("$root\生成结果输出\XXX.jpg", "$casePath\result.jpg", $true)
```

**人物库、场景库、品牌Logo等其他目录的图片也必须复制进来**（确保案例文件夹自包含）

---

### 步骤 A5：在案例文件夹内创建 prompt.txt

> ⚠️ **语言规范**：新入库的 `prompt.txt` **默认使用中文**（详见 `ai-designer-assistant.mdc`「默认输出语言规范」）。
> - Prompt 主体用中文书写，保留技术锚点原样：`image N` 编号、HEX 色号、API 参数、文件路径、品牌名、`BACKGROUND ONLY` / `PRODUCT ONLY` 等硬约束标记词
> - 标准化英文视觉术语可用"中文 + 英文括注"形式保留，如 `低角度仰拍 (low-angle shot)`
> - **例外**：用户明确要求以英文入库 / 复用的是现有英文案例且用户希望保留英文原版 → 可保留英文，并在开头注明"本案例 Prompt 原为英文，保留原文"
> - 存量英文 `prompt.txt` 文件**不主动批量改写**（避免破坏历史记录）

**prompt.txt 格式**：

```
[直接写入完整中文 Prompt 原文，一字不漏（保留 image N / HEX / API 参数 / 硬约束标记词原样）]

---
使用参考图：
  image 1 = img1-[描述].ext（[角色说明]）
  image 2 = img2-[描述].ext（[角色说明]）
  ...（有几张写几张）

API参数：
  --aspect-ratio [比例]
  --resolution [2k/4k]
```

```powershell
$promptContent = @"
[完整中文 Prompt，保留 image N 编号、HEX 色号、API 参数等技术锚点原样]

---
使用参考图：
  image 1 = img1-产品图.png（[角色]）
  image 2 = img2-[描述].png（[角色]）

API参数：
  --aspect-ratio 3:4
  --resolution 2k
"@
[System.IO.File]::WriteAllText("$casePath\prompt.txt", $promptContent, [System.Text.Encoding]::UTF8)
```

---

### 步骤 A6：在案例文件夹内创建 meta.txt

**meta.txt 格式**：

```
编号：P-XXX
归档日期：YYYY-MM-DD
分类：P[N] · [分类名称]

品牌：[品牌名 / 无品牌]
产品品类：[产品]
投放平台：[DTC / Amazon / 社媒]
宽高比：[比例]
分辨率：[2k/4k]

版式结构：[简要描述]
主要文字：[如有文案，列出主要文字]
配色方案：[背景色#hex / 强调色#hex / 文字色#hex]
品牌资产模式：[字体+色卡 / 含Logo / 无品牌]

参考图清单：
  img1-[描述].ext      → image 1（[角色]）
  img2-[描述].ext      → image 2（[角色]）
  ...

生成结果：result.[ext]

质量评分：★★★★☆
复用标签：#[标签1] #[标签2] #[标签3] ...

复用说明：
  可复用核心：[版式骨架/关键约束句等可直接复用的部分]
  需替换元素：[产品图/文案/颜色/人物等需要替换的部分]
  注意事项：[生成稳定性/特殊限制等提示]
```

---

### 步骤 A7：更新P域索引

读取 `参考库/P_生图成功案例库/P_域索引.txt`，执行两处更新：

**1. 在「关键词速查表」追加**（可追加多行，每行一组关键词组合）：
```
| #[关键词1] #[关键词2] #[关键词3] | P-XXX | P-XXX-[文件夹名] |
```

**2. 在「案例文件夹完整列表」追加**：
```
| P-XXX | P-XXX-[文件夹名] | P[N][类型] | [品牌] | [产品] | ★★★★☆ | YYYY-MM-DD |
```

**3. 更新文件头「记录数量」+1**

**4. 在对应分类子文件（P1~P5.txt）的「本分类案例列表」追加**：
```
| P-XXX | P-XXX-[文件夹名] | #[核心关键词] | ★★★★☆ | YYYY-MM-DD |
```

---

## 轨道 B：品牌设计案例归档（仅当品牌已激活时，在 A 完成后执行）

### 任务类型 → 写入文件路由表

| 任务类型 | 写入文件 |
|---|---|
| P1 模特场景图 / P3 产品展示图 / P5 白底产品图 | `品牌规范/[品牌名]/设计案例/电商_参考记录.txt` |
| P2 促销Banner/海报 | `品牌规范/[品牌名]/设计案例/广告投放_参考记录.txt` |
| P4 详情页模块 | `品牌规范/[品牌名]/设计案例/详情页_参考记录.txt`（不存在则自动创建） |

### 写入格式（简洁元数据，完整Prompt在P域文件夹）

```
### AI生成案例（P-XXX · YYYYMMDD）
- 文件：result.jpg（完整Prompt见 参考库/P_生图成功案例库/P-XXX-[文件夹名]/prompt.txt）
- 产品：[产品名称]
- 版式：[简要描述，1句]
- 文案：[主要文案，如有]
- 特点：[1-2句核心视觉特点]
- 尺寸：[宽高比] / [分辨率]
- 生成日期：[YYYYMMDD]
```

**同时复制 result.jpg 到** `品牌规范/[品牌名]/设计案例/`（文件名使用品牌规范命名如 `NEBILITY-AI-GEN-XXX-...jpg`）

---

## 轨道 C：向量索引增量更新（轨道 A 完成后必执行）

**执行时机**：轨道 A 的 A1-A7 全部完成之后,轨道 B 完成后(如激活)或跳过后,在最终确认输出之前。

**执行动作**（单条命令,Shell 同步等待）：

```powershell
python 工具/向量检索/update_index.py
```

该脚本按 text-hash 复用未变切片,只对本次新增的 P-XXX 案例(prompt.txt + meta.txt)调 Embedding API,典型成本 < ¥0.005、耗时 5-15s。完成后该成功案例即可被后续生图任务通过 `search.py --type P_CASE` 语义检索召回(参见 design-must-use-main-control.mdc 阶段 ③.5)。

**失败容错**:若脚本失败,在最终确认摘要中追加一行「⚠️ 向量索引未更新,本案例暂无法被语义检索命中,可下次入库时手动执行 `python 工具/向量检索/update_index.py`」,不阻塞入库流程收尾。

---

## 完成确认（三轨汇总输出格式）

两条轨道完成后，输出以下统一摘要：

```
✅ 双轨归档完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【轨道 A：P域入库】✅
  • 案例编号：P-XXX
  • 案例文件夹：参考库/P_生图成功案例库/P-XXX-[文件夹名]/
  • 文件夹内容：
    - prompt.txt（完整Prompt已保存）
    - meta.txt（元数据）
    - img1~imgN（[N]张参考图）
    - result.jpg（生成结果）
  • P域索引已更新（关键词速查表 + 案例列表）
  • 分类索引已更新：[P1~P5对应子文件]

【轨道 B：品牌设计案例归档】✅（或 N/A·无激活品牌）
 • 品牌：[品牌名]
 • 写入文件：[文件名]
 • 结果图：已复制到品牌设计案例目录

【轨道 C：向量索引增量更新】✅
 • 复用 X 条 / 重嵌 Y 条（耗时 Zs，成本约 ¥0.00Z）
 • 本案例已可被 search.py --type P_CASE 语义检索命中

下次同类任务时，系统将通过 P 域关键词速查表 + 向量语义检索**双通道**定位本案例的 prompt.txt 作为起点。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## ❌ 禁止行为

- **跳过轨道 C（向量索引更新），导致本次入库的成功案例无法被 search.py 语义检索召回**（P 域的核心复用价值依赖向量检索,跳过等于存档不检索）
- **只执行轨道 B（品牌案例归档）而跳过轨道 A（P域入库）**——这是最严重的违规
- 找不到Prompt时静默跳过P域入库（必须明确告知用户并索取Prompt）
- 将"存入案例库"解释为只保存到品牌`设计案例/`目录
- 在 prompt.txt 中省略或截断完整Prompt内容
- 未创建案例文件夹就试图写入文件
- 不复制参考图（案例文件夹必须自包含，不能只记录路径引用）
- 未完成 A 就开始执行 B

## ✅ 正确行为

- 识别触发词后，**立即宣布开始双轨归档**：「已识别入库触发，开始执行P域入库（轨道A）」
- 先确认完整Prompt是否在上下文（有则直接用，无则向用户索取）
- 按步骤A1→A7顺序创建文件夹、复制图片、写入文件、更新索引
- A轨道全部完成后，再执行B轨道
- 最终输出双轨汇总摘要
