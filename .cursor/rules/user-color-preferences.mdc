---
description: 用户个人色彩偏好——默认明亮色调、纯白背景，禁止灰色/灰黄/灰绿作为主色或背景
alwaysApply: true
---

# 用户色彩偏好（User Color Preferences · UCP）

## 核心原则

**在没有上位优先级明确覆盖的前提下，默认走用户个人色彩偏好**：明亮、纯净、高对比。白底优先，饱和色次之，低明度偏色调仅作辅助。

**上位优先级（本规则会让路的情况）**：
1. 品牌规范明确的色值（例如 `品牌规范/[品牌名]/品牌档案.txt` 中定义的主色/背景色）
2. 本轮用户明确指令（如"这张用暖米色背景"/"改成奶油色底"/"保留参考图的背景色"）
3. M2 配色决策卡 D 中**与本轮明确指令对齐**的色值

**下位默认（在上位没有明确指示时执行）**：本规则。

---

## 默认偏好声明

### 🎯 主色 / 背景色（默认强制）

| 用途 | 默认色 | 说明 |
|---|---|---|
| **背景 / 大面积底色** | `#ffffff` 纯白 | 所有生图任务的背景默认是纯白，除非有上位指令 |
| **次级背景 / 副画布** | `#fafafa` ~ `#f5f5f5` 近白 | 需要轻微层次时用极浅灰白，仍视为"白系" |
| **主色调基调** | 明亮、高明度、中高饱和 | 画面整体亮度 L ≥ 70，避免灰蒙蒙 |

### ✅ 允许作为辅色（占比 < 20%）

灰色、灰黄、灰绿、灰褐、暖米、杏仁米等低明度/低饱和偏色调，**仅允许作为**：
- 装饰几何块（分隔条、小徽章、小色块）
- 图标底色
- 文字阴影 / 细描边
- 小面积点缀

### ❌ 禁止作为主色 / 背景色

凡占比 ≥ 30% 的画面区域，禁止使用以下色调（除非有上位优先级明确指示）：

| 禁用色类 | 典型 HEX 示例 | 理由 |
|---|---|---|
| 中低明度中性灰 | `#8a8577` / `#999` / `#b0b0b0` | 压暗画面，不明亮 |
| 灰黄 / 卡其 / 橄榄灰 | `#c8b894` / `#a39665` / `#9c8f6e` | 显得陈旧、无生气 |
| 灰绿 / 苔绿 / 橄榄绿 | `#8b9a7b` / `#7a8a6f` / `#a8b09a` | 偏暗、压抑 |
| 灰褐 / 暖米（大面积）| `#e8e4dd` / `#d9d2c7` / `#c4b7a6` | 无品牌/参考图特殊要求时显旧 |
| 杏仁米 / 奶茶色（大面积）| `#e9dfc9` / `#dcc8a8` | 同上 |
| 任何 L ≤ 50 的暗色（大面积）| `#2a2a28` 纯墨、深褐等 | 用作文字或装饰可以，不能做大面积背景 |

---

## 触发条件与决策流程

### 场景 A：无上位指令（走默认偏好）

```
用户提出生图任务，未指定背景色/主色调
 ↓
品牌未激活 OR 品牌档案未明确背景色
 ↓
【默认执行】背景 = #ffffff 纯白，主色调走明亮路线
```

### 场景 B：参考图/版式图携带有色背景时（主动询问，不盲目继承）

```
用户提供了参考图，参考图背景是暖米/灰绿/杏仁米等非白色
 ↓
⚠️ 不得默认继承参考图背景色
 ↓
主动询问用户：
  "参考图的背景是 [色系描述]（#hex）。您希望：
   A. 保留参考图的 [色系] 背景
   B. 改用您默认的纯白 #ffffff 背景
   C. 使用其他特定色（请指定）"
 ↓
用户选择后再进入 ⑥.6 色卡生成
```

### 场景 C：品牌规范明确要求非白背景

```
品牌档案 / M2 决策卡明确指定非白背景（如 NEBILITY 黑底促销 Banner）
 ↓
品牌优先级高于本规则，按品牌指示执行
 ↓
在【色卡参考摘要】中标注：
  "本次背景为 [色值]（来自品牌规范 [具体文件]），与用户默认白底偏好不同，
   由品牌约束覆盖。"
```

### 场景 D：本轮用户明确要求非白色

```
用户本轮说"用暖米色/灰绿色做背景"
 ↓
明确指令优先级高于本规则，按用户本轮要求执行
 ↓
在【色卡参考摘要】中标注：
  "本次背景为 [色值]（来自用户本轮明确指令），与默认白底偏好不同。"
```

---

## 与其他规则的关系

| 规则 | 关系 |
|---|---|
| `design-must-use-main-control` | 在 ⑥.55（新增）用户色彩偏好对齐门与 ⑥.6 色卡生成门之间执行本规则的决策流程 |
| `color-palette-authority` | 色卡图生成时，角色标注 + 权威隔离仍然生效；本规则仅影响"哪些色值被选入色卡" |
| `brand-spec-loading` | 品牌规范明确的色值**优先于**本规则（场景 C） |
| `image-logic-check` | Pre-Gen L8（新增）检查背景色提议是否违反本规则；Post-Gen V6（新增）检查生成结果背景色是否合规 |

---

## Prompt 默认句式（无上位指令时）

```
背景颜色——干净的纯白背景，精确使用 #ffffff。
不是奶油色、不是米色、不是象牙白、不是暖灰色、不是卡其色、不是鼠尾草绿、不是橄榄色。
明亮、清爽、高调的摄影背景，均匀的中性棚拍灯光。

整体色彩基调——明亮、高调、清爽而现代。
产品色和品牌强调色是白色画布上的饱和视觉焦点。
任何低饱和/褪色/大地色调（卡其、鼠尾草绿、暖灰等）仅允许作为
小面积辅助点缀（占画面面积 < 20%），不得作为主色或背景色。
```

---

## ❌ 禁止行为

- **未询问用户就盲目继承参考图/版式图的有色背景**（如参考图是暖米，直接用暖米而不问用户）
- **用"为了匹配参考图整体调性"为由采用灰黄/灰绿/暖米作为主背景**
- 在没有品牌规范和用户本轮明确指令的情况下，默认选择非白色背景
- 生成图结果出现大面积灰黄/灰绿/暖米/杏仁米主背景，且【色卡参考摘要】中未标注"本次偏离默认偏好，原因是 XXX"
- 色卡图中把禁用色类（灰/灰黄/灰绿等）标注为 Background 角色，且占比 ≥ 30%

## ✅ 正确行为

- 所有生图任务默认假设背景是 `#ffffff` 纯白
- 遇到参考图带有色背景时，先向用户确认是"继承参考图色"还是"走默认纯白"（场景 B）
- 品牌规范/用户明确指令覆盖本规则时，在【色卡参考摘要】明确标注"偏离默认偏好，原因为 XXX"
- 灰色/灰黄/灰绿/暖米等仅出现在画面中作为辅助点缀，占比 < 20%，且标注为 Decor / Accent 角色
- Prompt 中主动写入默认句式（见上），明确禁用大面积灰系背景
