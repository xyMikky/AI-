---
description: 色卡权威与色彩角色隔离——色卡/M2配色决策卡是画面颜色的唯一权威来源，禁止让版式/风格参考图的色调牵引产品或人物照片
alwaysApply: true
---

# 色卡权威与色彩角色隔离（Color Palette Authority · CPA）

## 核心原则

**画面中任一像素的颜色，必须能回答两个问题：① 它属于什么角色？ ② 它的颜色来自哪张权威图？**

当一张画面同时包含**色卡图**（M2 配色决策卡 D · ⑥.6 生成）与**版式/风格参考图**时，**色卡是唯一的颜色权威**。版式/风格参考图只提供**结构、布局、装饰元素几何**，**不提供**产品色、肤色或整体照片色调。

**禁止**以「为了让照片与版式参考图色调协调」为由，对产品照片、模特皮肤、产品特写做降饱和 / 偏色 / 色调拉齐。产品色永远是产品色；背景色永远是背景色；装饰色永远是装饰色——三者在色卡中各司其职，在画面中互不污染。

---

## 触发条件

凡同时满足以下**两项**即强制执行本规范：

1. 任务调用 `rh-image-pro-img2img` 或 `rh-image-pro-txt2img`
2. `--images` 中包含下列任一组合：
   - 产品参考图（用户上传的原始产品/穿着图）
   - 版式 / 风格参考图（`image 3` 典型位，含装饰色块/背景色/版式骨架）
   - 色卡图（⑥.6 `color-palette-generator` 产出）

---

## 参考图的色彩权威分层

| 参考图类型 | 典型位 | 提供的色彩权威 | 不提供的色彩权威 |
|---|---|---|---|
| 产品参考图 | image 1 / image 2 | **产品的固有色**（如 cocoa brown `#4b2e27`、brand red `#bf192e`）、模特皮肤与发色、产品上的面料/蕾丝/缝线颜色 | 背景色、装饰色、画面整体色调 |
| 版式 / 风格参考图 | image 3 | **背景色**（如 warm cream `#e8e4dd`）、**装饰几何色块色**（如 warm gray `#8a8577`）、空间构图 | ❌ 产品色 ❌ 肤色 ❌ 产品特写的照片色调（**禁止将其色调叠加到产品照片上**） |
| 品牌资产图 | image 4 | 品牌字体色（如 `#2a2a28`）、品牌主色（如 Logo 红）、文字排版 | 背景色、产品色、装饰色 |
| 色卡图（权威） | image 5 | **所有 HEX 色号 + 角色 + 占比**，是画面所有颜色的最终裁决 | 不提供图形结构、布局 |

**裁决优先级（当多图色彩信号冲突时）**：
```
色卡图（image 5）  >  产品参考图的固有色（image 1/2）
                  >  品牌资产图的字体/品牌色（image 4）
                  >  版式参考图的装饰色块（image 3，仅限装饰元素）
```

---

## 色卡角色隔离原则（Color Role Isolation）

⑥.6 色卡图生成时，每个颜色必须标明「所属元素范围」。角色与像素的对应关系是**硬约束**：

| 色卡角色 | 只可应用于 | 不可应用于 |
|---|---|---|
| **Background**（背景色） | 整块空白画布、大面积底色 | 产品、模特肤色、装饰元素 |
| **Product**（产品色） | 产品本体的每一个像素（主体 + 缩略图 + 特写） | 背景、装饰色块、文字、他人服装 |
| **Decor / Accent**（装饰色） | 装饰几何（圆/方/胶囊/分隔条/图标底色） | 产品、模特皮肤、照片特写 |
| **Text / Ink**（文字色） | 文字层 | 产品、装饰色块（除非特意反白） |
| **Skin / Neutral**（肤色/中性辅色，可选） | 模特皮肤、中性内衣上衣等非核心产品 | 背景、装饰、核心产品 |

**关键推论**：
- 产品特写缩略图中的**产品像素**必须使用 Product 色，**不**能用 Decor 色去"统一色调"
- 小缩略图拍摄的光线可以简化，但产品色必须保持
- 不同位置出现的同一款产品，视觉上必须是同一种颜色

---

## Prompt 硬约束句式（强制）

> `image N` 编号、HEX 色号及大写硬约束标记词（`BACKGROUND ONLY` / `PRODUCT ONLY` / `DECORATIVE ELEMENTS ONLY` / `TEXT ONLY`）属于技术锚点，保留原样。

### 句式 A：产品色独立性声明（画面含产品照片时必写）

```
产品色保护区 (PRODUCT COLOR — PROTECTED ZONE)
画面中的所有产品像素——无论在主体模特身上、右侧的产品细节方格，还是任何特写缩略图中——精确呈现 image 1 / image 2 中的原始产品色（例如可可棕 #4b2e27）。
产品色独立于装饰色调，保持完全饱和，色相与 image 1 / image 2 精确一致。
产品是画面中唯一的暖色饱和元素，与整体低饱和装饰色调形成视觉张力——这种对比本身即是高级编辑感的来源。
```

### 句式 B：色彩角色隔离声明（有色卡图时必写）

```
色彩角色隔离 (COLOR ROLE ISOLATION, 源自 image N 色卡)
- #e8e4dd (暖米) → BACKGROUND ONLY，仅用于背景底色区域。
- #8a8577 (暖灰) → DECORATIVE ELEMENTS ONLY，仅用于胶囊、圆形、尺寸方格、小矩形、分隔条等装饰几何。
- #4b2e27 (可可棕) → PRODUCT ONLY，仅用于产品像素（主体 + 缩略图 + 特写）。
- #2a2a28 (深墨) → TEXT ONLY，仅用于主标题、标签、说明文字。
每种颜色精确对应其指定区域，色彩职责清晰区隔。
```

### 句式 C：特写缩略图色调保真（有产品特写或小图时必写）

```
特写产品色保真 (DETAIL THUMBNAIL COLOR FIDELITY)
右侧的小方格产品特写（肩部 / 腰部 / 腿部等）在干净中性的棚拍光下呈现真实的可可棕色 (#4b2e27)，色相与 image 1 保持一致，饱和度完全保留。
这些特写是产品的摄影记录，独立于装饰色调体系。
```

---

## Prompt 禁句清单（Pre-Gen 必查）

Prompt 中出现下列任一短语（**无论中文英文**）即视为违反本规范，**必须改写**后再提交 API：

| 触发短语 | 问题 | 正向替代写法 |
|---|---|---|
| `色调向暖灰褐色统一以协调整体画面` / `色调向装饰色靠拢` | 产品色被迫向装饰色靠拢，导致褪色 | `产品色精确呈现 image 1 / image 2 中的原色，在干净中性的棚拍光下保持完全饱和` |
| `降饱和以匹配装饰色调` / `降低饱和度以融入背景` | 强制产品降饱和 | `产品色保持完全饱和，色相与 image 1 精确一致` |
| `与背景色调融合` / `色相向场景偏移` | 产品色受背景牵引 | `产品色独立于背景色，保持其自身色调与饱和度，与 image 1 精确一致` |
| `所有元素色彩协调统一` / `整体色彩故事统一` | 泛化指令，AI 默认会降饱和产品 | `装饰元素共享低饱和色调；产品色独立保持饱和原色，二者形成设计张力` |
| `调成编辑感色调` / `杂志感调色`（未明确产品例外时） | 编辑感调色 = 产品褪色 | `编辑感氛围由装饰色调与光线构建；产品色保持原色，作为饱和焦点与之对比` |

> ⚠️ 即便是「统一氛围」「视觉协调」「让整体和谐」等善意初衷，也不得牺牲产品色。产品色与装饰色的**对比**本身就是高级编辑感的来源,不是「不协调」。

---

## 与其他规则的关系

| 规则 | 关系 |
|---|---|
| `design-must-use-main-control` | 本规范在 ⑥.6 色卡生成阶段强制执行角色隔离，在 ⑦ Prompt 构造阶段强制写入句式 A/B/C |
| `image-logic-check` | Pre-Gen L7（新增）检查禁句清单；Post-Gen V5-d（新增）强制对比产品色在不同位置的一致性 |
| `ref-image-must-include` | 本规范在「产品过度描述陷阱」之外，补充「参考图色调牵引陷阱」的防护 |
| `iterative-image-generation` | 迭代修正时，产品色失守 = 必须迭代，不得放行 |

---

## ❌ 禁止行为

- **以「和谐」「协调」「编辑感」为由，对产品照片做降饱和 / 色调拉齐**
- **让版式参考图（image 3）的装饰色或照片色调感染产品像素**
- Prompt 中写"色调向装饰色统一""降饱和以融入背景""与场景色调融合"等导致产品褪色的笼统指令
- 产品特写缩略图使用与版式参考图一致的低饱和灰褐色调（= 产品色被偷换）
- 色卡图中未声明颜色角色（只给 HEX 不标 Background / Product / Decor / Text）
- 让同一款产品在主体图和缩略图中呈现**可被肉眼察觉的色差**

## ✅ 正确行为

- 色卡图每个颜色旁标注角色：`#4b2e27 (产品可可棕，5%)` / `#8a8577 (装饰暖灰，15%)` / `#e8e4dd (背景暖米，55%)` / `#2a2a28 (文字深墨，25%)`
- Prompt 中写入句式 A（产品色独立）+ 句式 B（角色隔离）+（含特写时）句式 C（特写防色调牵引）
- 产品主体与产品特写缩略图使用**相同**的 Product 色（不允许"为了协调而调色"）
- 装饰元素（圆/方/胶囊/分隔条）严格使用 Decor 色，不得侵占 Product 色
- Post-Gen V5-d 强制肉眼对比：主体产品色 vs 缩略图产品色 vs 产品参考图色——视觉上是同一种颜色
