---
description: 提交给生图模型的 Prompt 必须是纯视觉语言、最精简、不抢参考图话语权；禁止内部标识（X-XXX/M编号/SCENE-5/T1等）、禁止 IMAGE ROLES 目录块、禁止用文字重新命名参考图里已有的颜色/材质/结构；优先正向表述（"保持背景不变"而非"不要改变背景"）
alwaysApply: true
---

# Prompt 卫生规范：四条铁律

**Prompt 的每一个 token 都在和参考图竞争模型注意力。** 多余的元信息、目录说明、对参考图已承载信息的文字复述，都会稀释模型对"图像里长什么样"的读取权重，导致色偏、结构漂移、细节丢失。

本规则列出四条铁律：① 禁止内部标识 · ② 禁止 IMAGE ROLES 目录块 · ③ 禁止用文字重新命名参考图已承载的视觉属性 · ④ 优先正向表述。

---

## 铁律一：禁止内部标识污染

**提交给 Nano Banana / Gemini 等生图模型的 Prompt，是给模型读的视觉指令，不是给系统读的审计日志。**

凡是系统内部用于调度、审计、溯源、模块互引的标识符，**一律不得出现**在最终 `--prompt` / `--prompt-file` 的正文或注释中。模型不认识这些符号，只会将其当作噪声或错误地尝试"视觉化"它们。

---

## ❌ 禁止出现在 Prompt 中的内部标识（不完全清单）

| 类别 | 示例 | 原因 |
|---|---|---|
| 参考库案例编号 | `X-001`, `X-002`, `P-003`, `N-012`, `E-031` | 模型不知道这是案例编号，可能理解为尺码/型号 |
| 模块编号 | `M8`, `M16`, `Pre-Gen L6-f`, `⑥.6` | 系统流程标签，与视觉无关 |
| 规则文件名 | `image-logic-check.mdc`, `SCENE-5`, `color-palette-authority.mdc` | 内部文件引用 |
| 互动谱代号 | `T1`, `T2`, `T6`, `B1`, `P3`, `SI-1`, `SI-2` | 姿势/场景分类代号 |
| 元注释短语 | `anti X-001 / X-002`, `防护 X-002`, `X4 构图过松对策` | 审计溯源标记 |
| 中文/双语元注释 | `# 对抗过紧陷阱`, `// 防头发遮挡`, `[规避 waist-up 陷阱]` | 模型会把注释当文字渲染进画面，或干扰理解 |
| 评分/权重元数据 | `Q6 产品视觉权重`, `M16 Q1=23` | 内部评分维度 |

---

## ✅ 正确做法：把"为什么"翻译成"画面要什么"

规则体系（如 `design-must-use-main-control.mdc` ③.6）要求根据 X 域反向查询加入规避约束句——**这个约束句本身必须是纯视觉语言**，不能携带溯源标签。

### 错误示范（污染版）

```
【取景构图 — 规避 X-001 / X-002】     ← 含内部案例编号
产品必须占画面 55-65%...
【发型 — SCENE-5 防遮挡】             ← 含内部章节代号
发尾梳至一侧...
```

### 正确示范（干净版）

```
【取景构图】
产品躯干区域占画面 55-65%，取景下边界落在髋线，上边界落在肩线以上，产品完整可见。

【发型位置】
发尾梳至一侧或扎起，双侧袖口完整可见。
```

**翻译原则**：X/P 域教会你"这次要约束什么"——把约束转成**模型能理解的正向视觉指令**，描述目标状态而非禁止项，然后把内部编号**丢掉**，不要带进 Prompt。

---

## 内部溯源信息的正确去处

需要记录"本次 Prompt 为何包含某条约束"时，写在以下位置**之一**，不得写进 Prompt 正文：

1. **Pre-Gen 报告的【陷阱规避清单】**（assistant 回复正文中，用户可见但不喂给模型）
2. **案例入库时的 `meta.txt`**（归档溯源用，不参与生图）
3. **Prompt 文件开头的独立元数据区**（仅当该区域**不会被** `--prompt-file` 读入主体时；当前 `generate_image.py` 会全文读取，因此**此方式暂不可用**）

**当前实现下：Prompt 文件从第一行到最后一行都会被提交给模型，没有"注释区"。所有内部标识必须被剔除。**

---

## 自检清单（提交生图前必查）

在调用 `generate_image.py` 之前，对 Prompt 文件做一次搜索：

- [ ] 是否包含 `X-`、`P-`、`N-`、`E-`、`F-`、`H-` 等开头的编号？
- [ ] 是否包含 `M1`~`M18`、`⓪`~`⑨`、`L6`、`Q1`~`Q6` 等模块/流程标签？
- [ ] 是否包含 `.mdc` / `SCENE-` / `SI-` / `T1`~`T6` / `B1`~`B3` / `P1`~`P7` 等规则代号？
- [ ] 是否包含 `anti / 对抗 / 规避 / 防护 XXX` 这类元注释句式？
- [ ] 中文注释（`#`、`//`、`【】内仅给自己看的话`）是否全部清除？

任一命中 → 改写或删除该行，再提交。

---

## 与其他规则的关系

- **`design-must-use-main-control.mdc` ③.6** 要求加入反向约束句 → 本规则补充说明：约束句必须是**纯视觉语言**，不携带 `X-XXX` 溯源标签
- **`nano-banana-prompt-guide`** 场景六的 `[LAYOUT]` / `[NEGATIVE]` 等分块标签属于**视觉分块**（模型可理解为"下面是排版说明/排除项"），不在本规则禁用范围
- **`ref-image-must-include.mdc`** 要求的 `image 1`/`image 2` 编号是**模型约定接口**，必须保留

---

## 铁律二：禁止 IMAGE ROLES 目录块

### 为什么

```
[图片角色目录]
image 1 = 产品参考（短袖高领连体衣，网纱育克设计）
image 2 = 仅作为模特身份参考——仅借鉴面部特征……
image 3 = 仅作为场景参考——空间布局、建筑结构……
```

这类"目录块"是写给审计员/复盘者看的。对生图模型而言：

1. **它把参考图再用文字描述了一遍**（"short-sleeve mock-neck bodysuit with sheer mesh yoke"），构成"产品过度描述陷阱"（详见 `ref-image-must-include.mdc`）——文字描述与图像竞争，导致生成结果偏离参考图
2. **它占用了本该用于视觉指令的 token 预算**
3. **后文已经会用 `image N` 内联引用**（如 `keep the garment from image 1 exactly`），不需要前置声明
4. **"ONLY"、"do NOT copy the clothing"** 这类排除项在"人物/场景引用句"里一句话就能带过，不需要独立章节

### 正确做法：内联引用，用到时说明

每张图的角色声明**直接写在使用它的约束句里**，而不是前置单独列表：

```
# ✅ 正确（内联引用）
产品与 image 1 中完全一致。
使用 image 2 中人物的面容、发型和身材——服装和背景以各自对应的参考图为准。
按照 image 3 重建环境——墙面、长椅和地板，不复制场景中的人物。

# ❌ 错误（前置目录块 + 后文再重复一遍）
[图片角色目录]
image 1 = 产品参考...
image 2 = 仅作为模特身份参考...
image 3 = 仅作为场景参考...

[产品]
产品与 image 1 中保持一致...
```

### 例外

当传入 **≥ 5 张图** 且其中**至少有 2 张容易被模型混淆角色**（例如两张都是人物但一张是身份参考、另一张是姿势参考），可以保留**极简版** IMAGE ROLES（每张一句，不得重复描述画面内容）：

```
image 1 = 产品。image 2 = 面部参考。image 3 = 姿势参考。image 4 = 场景。image 5 = 色卡。
```

4 张及以下图片**始终禁用**目录块。

---

## 铁律三：禁止用文字重新命名参考图已承载的视觉属性

### 原理

参考图里**已经有**的视觉信息（颜色、材质、结构、纹理、廓形、**版式、构图、模块排布**），**不要用文字再命名一次**。只说"保持/来自 image N"。

> **适用对象**：产品参考图、模特参考图、场景参考图、**版式参考图（详情页/A+截图/多模块竞品长图）**、构图参考图——但凡参考图里"看得见的东西"，都属于此铁律管辖范围。

**最典型的翻车场景——颜色词汇化**：

```
# ❌ 错误（给颜色起名字）
所有产品像素必须精确保留 image 1 中的巧克力棕色调。
保留 image 1 中的可可棕塑身衣。
使用参考图中的暖焦糖色。

# ✅ 正确（指向图片，不命名）
所有产品像素精确保留 image 1 中的颜色——色相、饱和度、明度完全一致，色调与 image 1 精确一致。
产品与 image 1 中完全一致，保留其原始颜色。
```

**为什么"chocolate brown"有害**：

- 模型对"chocolate brown"有**自己的先验分布**（偏橙红？偏冷棕？偏黑？），这个先验会和 image 1 的实际像素**打架**
- 模型会尝试**协调**两者，结果生成出一个"既像图片又像文字意图"的中间色——这就是色偏的根源
- 参考图里的颜色**已经是最精确的定义**（RGB 每个像素都在那儿），文字命名只会引入噪声

### 适用范围（不止颜色 — 包含版式/构图）

| 参考图已承载的属性 | ❌ 文字重命名（有害） | ✅ 图片锚定（正确） |
|---|---|---|
| 颜色 | `巧克力棕` / `可可色` / `暖焦糖色` | `image 1 中的精确颜色` |
| 材质/面料 | `网纱育克配不透明莱卡主体` | `image 1 中的面料和分片结构` |
| 结构细节 | `高立领短袖设计` | `image 1 中的领口、袖型和廓形` |
| 发色 | `自然金棕色波浪卷发` | `image 2 中的发色和发质` |
| 肤色 | `白皙欧美肤色` | `image 2 中的肤色` |
| 场景材质 | `条纹木地板和白色长椅` | `image 3 中的地板、长椅和墙面` |
| **版式/模块排布** | `5 个堆叠区域：品牌顶部 10%，主视觉分栏 35%（50:50），4 格矩阵 25%，新品条幅 15%，底部 15%……` | `完整复刻 image 4 中的多区块竖向模板——区块数量、叠加顺序、模块形状、比例关系保持一致` |
| **构图网格** | `3 列网格，24px 间距，中栏宽 1.5 倍，基于 12pt 基线对齐` | `image 4 的列网格与对齐方式` |
| **模块形状** | `右上角直径 220px 的圆形模特插图，4 列各带 8px 圆角的产品缩略图` | `image 4 中的圆形模特插图和 4 格缩略图行` |
| **文字层级** | `章节标题 48pt Archivo Bold 全大写深炭色居中，下方 14pt 副标题全大写字间距 0.2em……` | `image 4 中的标题/副标题/正文层级体系` |
| **装饰元素** | `每个章节标题下一条 2px 红色横线，60px 长，颜色 #bf192e` | `image 4 中的红色强调下划线——位置精确一致` |
| **分隔节奏** | `区块 1 和 2 之间 80px 纵向留白，2 和 3 之间 120px……` | `image 4 的纵向留白节奏` |

**版式过度描述的典型翻车模式**：一次写 5 个 SECTION × 每个 5-10 行英文 = 300-800 token 的长篇版式指令。模型注意力预算被文字吃掉，参考图被降权，结果"像 Prompt 想象的版式"而不是"像参考图的版式"。

> 正确做法详见 `layout-reference-analyzer` Skill 的 **Phase 5 三件套**（版式引用句 + 差异点清单 + 关键锚点约束），和 `ref-image-must-include.mdc` 的"参考图过度描述陷阱"章节。

### 例外：有色卡图时的 HEX 引用

当 Prompt **同时**传入色卡图（`color-palette-generator` skill 生成）时，可以用 HEX 精确引用——因为此时色卡图本身就是"权威色"的视觉来源：

```
# ✅ 正确（有色卡图时）
精确使用 #4A2E27（产品色，来自 image 4 色卡）——区别于场景调色板中的灰色
```

此场景下，HEX 码对应色卡图上**确实存在**的那个方块，不是对产品参考图里颜色的文字重命名。

### 仅限于：方向/角度/焦点的补充说明可以写

文字能补充的只有**参考图没明说的相对关系**：

```
# ✅ 正确（补充方向/角度，图片里没有的信息）
产品与 image 1 中完全一致——展示背面视角。
产品与 image 1 中完全一致——特写腰部接缝区域。
```

---

## 铁律四：优先正向表述

**告诉模型"画面要呈现什么"，而不是"不要出现什么"。** 当代主流生图模型对正向指令的执行比负向约束更稳定——模型需要先激活被禁止的视觉概念，再试图压制它，这反而增加输出概率。

### 常见转换对照

| ❌ 负向约束 | ✅ 正向表述 |
|---|---|
| 不要改变背景 | 背景与 image N 保持一致 |
| 不要改变产品颜色 | 产品色精确保留 image 1 原色 |
| 禁止降饱和 | 色彩保持完全饱和 |
| 禁止偏色 | 色相与 image 1 精确一致 |
| 不要让头发遮挡肩部 | 发尾梳至一侧，双侧袖口完整可见 |
| 不要裁切太紧 | 取景下边界落在髋线，产品躯干完整可见 |
| 不要让产品颜色向背景靠拢 | 产品色独立于背景色，保持其自身色调与饱和度 |
| 不要重复产品细节 | 产品外观以 image 1 为准，文字仅指向图片 |

### 例外：允许保留负向表述的情况

以下情况因为没有精确的正向等价表达，允许保留负向形式，但须与正向约束配对：

- **精确尺寸边界**：`取景不超过腰线以上`（等价正向较冗长时）
- **色彩角色硬约束标记词**：`BACKGROUND ONLY` / `PRODUCT ONLY` 等全大写技术信号保留原样（不属于自然语言负向约束）
- **排除干扰元素**：`画面中只出现产品和模特，无其他人物`（"只出现"是正向，隐含排他，可接受）

### 自检

提交前检查 Prompt 中是否存在"不要/禁止/不得/DO NOT/NOT allowed/NEVER"等负向关键词——每出现一处，判断能否用正向等价句替换。能替换的立即替换，不能替换的保留并与正向约束配对。

---

## 合并自检清单（提交生图前必查）

在调用 `generate_image.py` 之前，对 Prompt 文件做一次搜索：

- [ ] 是否包含 `X-`、`P-`、`N-`、`E-`、`F-`、`H-` 等开头的内部编号？
- [ ] 是否包含 `M1`~`M18`、`⓪`~`⑨`、`L6`、`Q1`~`Q6` 等模块/流程标签？
- [ ] 是否包含 `.mdc` / `SCENE-` / `SI-` / `T1`~`T6` / `B1`~`B3` / `P1`~`P7` 等规则代号？
- [ ] 是否包含 `anti / 对抗 / 规避 / 防护 XXX` 这类元注释句式？
- [ ] 是否存在 **IMAGE ROLES 目录块**（且传入图片 ≤ 4 张）？
- [ ] 是否存在**颜色/材质/结构的文字重命名**（如 `chocolate brown` / `sheer mesh` / `golden-brown hair`）而非 `the X from image N`？
- [ ] 是否存在**版式/构图/模块排布的文字复述**（如逐 SECTION 写 5-10 行英文版式描述、给出列宽比/模块高度百分比/元素像素位置）？有版式参考图时应只写"版式引用句 + 差异点清单 + 关键锚点"三件套，而非详细版式翻译。
- [ ] Prompt 总长是否超过 400 token？版式类任务 Prompt 理想长度 200-300 token；超长通常意味着发生了过度描述。
- [ ] 中文注释（`#`、`//`、`【】仅给自己看的话`）是否全部清除？
- [ ] 是否存在可替换为正向表述的负向约束（`不要/禁止/不得/DO NOT/NOT allowed/NEVER`）？能替换的立即替换，不能替换的与正向约束配对使用。

任一命中 → 改写或删除该行，再提交。
