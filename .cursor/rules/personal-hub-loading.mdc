---
description: 个人中心自动加载（Personal Hub Auto-Loading）——任意任务前加载使用者档案+工作摘要；档案为空时强制优先补全最小身份信息完成绑定；四轴优先级栈
alwaysApply: true
---

# 个人中心自动加载（Personal Hub Auto-Loading）

## 核心原则

**个人中心让智能体「认识这个人」**：不仅知道品牌怎么设计，还知道使用者是谁、在做什么、接下来要做什么。

V9.0 定位升级：本系统从「AI 设计师助手」扩展为**全公司各部门、各岗位、每个人的可成长辅助助手**。设计能力完整保留；个人中心为**全任务上下文层**。

**本系统设定为「必须绑定使用者」**：智能体的辅助质量依赖于对使用者身份与工作状态的了解。因此当激活使用者的个人档案为空时，**优先补全使用者信息再继续任务**（见下方「强制绑定门」）。

---

## ⚡ 强制绑定门（档案为空 → 优先补全使用者信息）

> 优先级高于本规则其余所有加载流程，也高于普通任务执行。

### 触发判定：什么算「档案为空」

满足以下任一条件即判定为「未绑定」：

- `个人档案.txt` 头部「档案完整度：0/4」
- 身份字段（姓名/昵称、部门、岗位）**三项仍全为 `[待填写]`**
- 使用者目录刚由模板初始化、`基础信息/` 四文件均未填充

### 门行为：先绑定，再办事

```
任务开始 → 个人中心加载 → 检测激活使用者档案
  ↓
档案为空（未绑定）？
  ├─ 否 → 正常加载个人上下文，继续任务
  └─ 是 → 进入强制绑定门：
       ① 一句话说明：本助手需先认识你才能更好地辅助
       ② 调度 profile-spec-learner（onboarding 模式），
          用 AskQuestion 一次性收集【最小身份信息】
       ③ 写入个人档案 + 基础信息/岗位与职责，完整度 ≥ 1/4
       ④ 标记「已绑定」→ 继续用户原本的任务
```

### 最小身份信息（3 项即可解锁，完整基础信息可后续补）

| 字段 | 写入位置 |
|---|---|
| 姓名 / 昵称 | 个人档案.txt · 身份概览 |
| 部门 | 个人档案.txt + 基础信息/岗位与职责.txt |
| 岗位 | 个人档案.txt + 基础信息/岗位与职责.txt |

> 解锁后可继续原任务；偏好 / 技能 / 协作关系 / 工作日志属于增量补全，不阻断。

### 强制绑定门的豁免（仅这些情况可跳过绑定）

1. **本轮任务就是录入档案 / 日志本身**：`初始化个人档案` / `更新我的信息` / `记录工作日志`（这些任务自身就在补全，不重复拦截）
2. **用户明确声明**：`匿名模式` / `跳过绑定` / `先不填` / `不要用我的个人档案`
3. **纯系统维护**：logic-chain-diagnostic / skill-converter / ai-persona-architect
4. **个人中心未初始化**（目录不存在）：先引导用户「初始化个人档案」创建目录

### 降级与不打扰原则

- 用户选择「先跳过 / 匿名」→ 本会话按匿名进行，**记录该选择**，同一会话内**不再反复追问**
- 新会话开始若仍未绑定，可**轻量提醒一次**（一句话），用户再次跳过即尊重，不纠缠

---

## 触发条件

以下情形**自动执行**个人中心检测与加载：

1. **每次新会话**或用户发起任意工作任务（设计 / 运营 / 复盘 / 文档 / 分析等）
2. 用户显式触发：`切换使用者` / `我的档案` / `我的待办` / `工作状态`
3. 用户录入或更新：`记录工作日志` / `更新个人档案`（走对应 Skill，完成后刷新加载）

**豁免**（可不加载个人中心）：

- 纯系统维护（logic-chain-diagnostic / skill-converter / ai-persona-architect）
- 用户明确「不要用我的个人档案」/「匿名模式」
- 个人中心目录不存在或未初始化（提示用户运行 profile-spec-learner）

---

## 使用者检测协议

```
任务开始
  ↓
读取 个人中心/个人中心主控.txt
  ├─ 有「当前激活使用者」→ 验证目录存在
  ├─ 用户显式指定使用者 → 覆盖默认激活
  └─ 目录不存在 → 提示：说「初始化个人档案」创建
  ↓
检测档案是否为空（见「强制绑定门」触发判定）
  ├─ 未绑定且不在豁免清单 → 先走强制绑定门
  └─ 已绑定 → 加载个人上下文（轻量）
```

### 验证命令

```powershell
$root = $PWD.Path
$userName = "默认用户"  # 或主控中激活的使用者
$hub = Join-Path $root "个人中心"
$userDir = Join-Path $hub $userName
$profile = Join-Path $userDir "个人档案.txt"
$summary = Join-Path $userDir "工作日志\工作摘要.txt"
[System.IO.File]::Exists($profile)
[System.IO.File]::Exists($summary)

# 档案为空判定：身份三项是否仍为 [待填写]
$txt = [System.IO.File]::ReadAllText($profile, [System.Text.Encoding]::UTF8)
$isEmpty = ($txt -match "档案完整度：0/4") -or ($txt -match "姓名 / 昵称：\[待填写\]")
$isEmpty
```

---

## 加载协议

### Always-Load（强制 · 轻量）

| 文件 | 大小限制 | 用途 |
|---|---|---|
| `个人中心/[使用者]/个人档案.txt` | <3KB | 身份、偏好速查、工作上下文摘要 |
| `个人中心/[使用者]/工作日志/工作摘要.txt` | 建议 <5KB | 当前状态、待办、近期重点 |

加载后输出简短确认（可合并进【调控中心决策】）：

```
【个人上下文已加载】
• 使用者：[名] · [岗位] · [部门]
• 当前状态：[工作摘要一句话]
• 待办 Top3：[列表]
```

### 按任务类型追加加载

| 任务类型 | 追加文件 |
|---|---|
| 设计 / 生图 / 详情页 | + `基础信息/个人偏好.txt` |
| 工作规划 / 周报 / 复盘 | + `工作日志/日志索引.txt` + 近期 1-3 条日志 |
| 跨部门协作 | + `基础信息/协作关系.txt` |
| 岗位 SOP / 方法论 | + `基础信息/岗位与职责.txt` |

---

## 优先级栈（V9.0 四轴）

```
用户本轮明确指令
  > 品牌规范（品牌激活时的设计硬约束）
  > 个人中心（岗位上下文 + 工作节奏 + 个人偏好）
  > 场景知识库（市场/平台正确性）
  > 参考库 + 通识原则（审美与通用设计原则）
```

**冲突处理示例**：

| 冲突 | 裁决 |
|---|---|
| 品牌要求黑底 vs 个人偏好白底 | 品牌规范优先（设计任务） |
| 工作摘要显示本周赶 deadline vs 用户要深度引导 | 可走 Express 模式，标注「已知时间紧」 |
| 日志待办与本轮用户请求矛盾 | 先完成用户本轮指令，顺带提醒待办 |

---

## 与工作日志的协同

- 用户说「记录工作日志」→ 路由 `work-log-manager`，**不得**用 profile-spec-learner 代替
- 归档后**必须**刷新 `工作摘要.txt`，下次加载才反映最新状态
- 批量导入历史日志后，工作摘要应覆盖「入职至今」的项目脉络

---

## 与品牌规范的对称关系

| 能力 | 品牌规范 | 个人中心 |
|---|---|---|
| 学习 Skill | brand-spec-learner | profile-spec-learner |
| 核心档案 | 品牌档案.txt | 个人档案.txt |
| 素材入口 | 00_待学习/ | 00_待学习/ + 工作日志/00_待归档/ |
| 行为记录 | 设计案例/ | 工作日志/按年/ |
| 滚动摘要 | — | 工作摘要.txt |

---

## ❌ 禁止行为

- **档案为空（未绑定）且不在豁免清单时，跳过强制绑定门直接执行长篇任务**
- 强制绑定时一次性追问过多字段（超出最小身份 3 项，把用户劝退）
- 用户已选「匿名 / 先跳过」后，同一会话内反复追问绑定
- 个人中心已初始化但**从不加载**档案就开始长篇任务
- 将工作日志内容写入品牌规范目录
- 捏造用户未提供的待办或岗位信息
- 多使用者场景下**混用** A 的档案回答 B 的问题

## ✅ 正确行为

- 新会话开始读取个人档案 + 工作摘要
- **档案为空时先走强制绑定门**：一句话说明 + AskQuestion 收集最小身份 → 写入 → 继续原任务
- 尊重用户「匿名 / 先跳过」选择，降级为匿名会话且不纠缠
- 输出【个人上下文已加载】（可精简为一行）
- 工作类回答引用摘要中的待办与状态
