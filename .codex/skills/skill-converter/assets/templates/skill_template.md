---
name: [skill-name]
description: [核心动作 + 适用范围 + 触发场景 + 边界]。适用于[场景1]、[场景2]、[场景3]。只做[范围内任务]，不做[边界外任务]。关键词：[词1]、[词2]、[词3]、[词4]、[词5]。
---

# [Skill 标题]

## 定位

[1-2 句话说明这个 Skill 的核心目的、边界和典型输入输出。]

## 工作流程

1. [读取或确认输入]
2. [执行核心处理]
3. [生成产物或反馈]
4. [验证结果]

## 资源使用

- 需要长规范时读取 `references/[FILE].md`。
- 需要确定性处理时运行 `scripts/[script_name].py`。
- 需要模板或静态资源时使用 `assets/[path]`。

## 使用方法

- 输入：[输入要求]
- 输出：[输出文件 / 报告 / 结构化数据]
- 约束：[必须遵守的限制]

## 示例

用户说：[典型请求]

处理：
- [步骤摘要]
- [验证摘要]
